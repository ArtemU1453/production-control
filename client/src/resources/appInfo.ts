/** Static application metadata surfaced in Settings, About and Diagnostics.
 *  `version` is the marketing/semantic version; `build` increments per release
 *  build; `bundleId` mirrors the reverse-DNS identifier used when packaging the
 *  app for distribution. */
export const appInfo = {
  version: "1.0.0",
  build: "1",
  bundleId: "com.narezka.calculator",
} as const;
